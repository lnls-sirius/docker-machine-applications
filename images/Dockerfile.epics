ARG BASE_IMAGE_TAG
ARG ANSIBLE_TAG
FROM dockerregistry.lnls-sirius.com.br/fac/fac-base:${BASE_IMAGE_TAG}

SHELL ["bash", "-c"]

WORKDIR /opt

RUN set -e; \
    set -x; \
    rm -rf lnls-ansible; \
    git clone https://github.com/lnls-sirius/lnls-ansible.git; \
    cd lnls-ansible; \
    git checkout master; \
    \
    ansible-playbook --connection=local playbook-servers-ioc-docker-epics.yml

RUN source /etc/profile

CMD ["/bin/bash"]
